{
  "name": "aster",
  "version": "1.0.0",
  "private": true,
  "description": "A fast, local-first Kubernetes desktop workbench.",
  "author": "Aster contributors",
  "type": "module",
  "license": "Apache-2.0",
  "packageManager": "pnpm@10.12.2",
  "scripts": {
    "build": "pnpm --dir apps/desktop build",
    "check": "pnpm typecheck && pnpm test && go -C core test -race ./... && go -C core vet ./... && pnpm build && pnpm --dir apps/desktop test:rust",
    "dev": "pnpm --dir apps/desktop dev",
    "dist": "pnpm --dir apps/desktop dist",
    "landing:dev": "pnpm --dir apps/landing dev",
    "landing:build": "pnpm --dir apps/landing build",
    "test": "pnpm --dir apps/desktop test",
    "typecheck": "pnpm --dir apps/desktop typecheck"
  },
  "engines": {
    "node": ">=22.19.0"
  }
}
